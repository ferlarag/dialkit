// @generated by codegen/contracts/generate_pagination.py.
use dialkit_messaging_generated::{models, pagination::PageResponse};

#[test]
fn adapter_list_alpha_sender() {
    let response: models::ListAlphaSenderResponse = serde_json::from_str(
        r#"{"alpha_senders":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_brand_registrations() {
    let response: models::ListBrandRegistrationsResponse = serde_json::from_str(
        r#"{"data":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_brand_vetting() {
    let response: models::ListBrandVettingResponse = serde_json::from_str(
        r#"{"data":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_channel_sender() {
    let response: models::ListChannelSenderResponse = serde_json::from_str(
        r#"{"senders":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_destination_alpha_sender() {
    let response: models::ListDestinationAlphaSenderResponse = serde_json::from_str(
        r#"{"alpha_senders":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_phone_number() {
    let response: models::ListPhoneNumberResponse = serde_json::from_str(
        r#"{"phone_numbers":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_service() {
    let response: models::ListServiceResponse = serde_json::from_str(
        r#"{"services":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_short_code() {
    let response: models::ListShortCodeResponse = serde_json::from_str(
        r#"{"short_codes":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_tollfree_verification() {
    let response: models::ListTollfreeVerificationResponse = serde_json::from_str(
        r#"{"verifications":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_us_app_to_person() {
    let response: models::ListUsAppToPersonResponse = serde_json::from_str(
        r#"{"compliance":[{}],"meta":{"next_page_url":"/next?PageToken=a%2Fb%2Bc%3D"}}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
