# dialkit-core

Shared authentication, HTTP execution, retries, errors, tracing, and pagination for dialkit crates.
This support crate is published so the generated companion and stable facade can share one transport
policy. Ordinary applications should depend on `dialkit` instead. Generation and release provenance
is included in `PROVENANCE.md`.
