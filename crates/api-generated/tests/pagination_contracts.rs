// @generated by codegen/contracts/generate_pagination.py.
use dialkit_api_generated::{
    apis::configuration::Configuration,
    models,
    pagination::{PageResponse, follow},
};
use dialkit_core::{
    auth::{AccountSid, Credentials},
    error::Error,
    request::{ClientConfiguration, HttpClient},
    retry::RetryPolicy,
};
use secrecy::SecretString;
use std::time::Duration;
use url::Url;
use wiremock::{
    Mock, MockServer, ResponseTemplate,
    matchers::{header_exists, method, path, query_param},
};

#[test]
fn adapter_list_application() {
    let response: models::ListApplicationResponse = serde_json::from_str(
        r#"{"applications":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_call() {
    let response: models::ListCallResponse =
        serde_json::from_str(r#"{"calls":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_call_event() {
    let response: models::ListCallEventResponse =
        serde_json::from_str(r#"{"events":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_call_notification() {
    let response: models::ListCallNotificationResponse = serde_json::from_str(
        r#"{"notifications":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_call_recording() {
    let response: models::ListCallRecordingResponse = serde_json::from_str(
        r#"{"recordings":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_conference() {
    let response: models::ListConferenceResponse = serde_json::from_str(
        r#"{"conferences":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_conference_recording() {
    let response: models::ListConferenceRecordingResponse = serde_json::from_str(
        r#"{"recordings":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_incoming_phone_number() {
    let response: models::ListIncomingPhoneNumberResponse = serde_json::from_str(
        r#"{"incoming_phone_numbers":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_incoming_phone_number_local() {
    let response: models::ListIncomingPhoneNumberLocalResponse = serde_json::from_str(
        r#"{"incoming_phone_numbers":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_incoming_phone_number_mobile() {
    let response: models::ListIncomingPhoneNumberMobileResponse = serde_json::from_str(
        r#"{"incoming_phone_numbers":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_incoming_phone_number_toll_free() {
    let response: models::ListIncomingPhoneNumberTollFreeResponse = serde_json::from_str(
        r#"{"incoming_phone_numbers":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_media() {
    let response: models::ListMediaResponse = serde_json::from_str(
        r#"{"media_list":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_member() {
    let response: models::ListMemberResponse = serde_json::from_str(
        r#"{"queue_members":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_message() {
    let response: models::ListMessageResponse =
        serde_json::from_str(r#"{"messages":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_outgoing_caller_id() {
    let response: models::ListOutgoingCallerIdResponse = serde_json::from_str(
        r#"{"outgoing_caller_ids":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_participant() {
    let response: models::ListParticipantResponse = serde_json::from_str(
        r#"{"participants":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_queue() {
    let response: models::ListQueueResponse =
        serde_json::from_str(r#"{"queues":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_recording() {
    let response: models::ListRecordingResponse = serde_json::from_str(
        r#"{"recordings":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_recording_add_on_result() {
    let response: models::ListRecordingAddOnResultResponse = serde_json::from_str(
        r#"{"add_on_results":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_recording_add_on_result_payload() {
    let response: models::ListRecordingAddOnResultPayloadResponse =
        serde_json::from_str(r#"{"payloads":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_recording_transcription() {
    let response: models::ListRecordingTranscriptionResponse = serde_json::from_str(
        r#"{"transcriptions":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_short_code() {
    let response: models::ListShortCodeResponse = serde_json::from_str(
        r#"{"short_codes":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_auth_calls_credential_list_mapping() {
    let response: models::ListSipAuthCallsCredentialListMappingResponse =
        serde_json::from_str(r#"{"contents":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_auth_calls_ip_access_control_list_mapping() {
    let response: models::ListSipAuthCallsIpAccessControlListMappingResponse =
        serde_json::from_str(r#"{"contents":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_auth_registrations_credential_list_mapping() {
    let response: models::ListSipAuthRegistrationsCredentialListMappingResponse =
        serde_json::from_str(r#"{"contents":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_credential() {
    let response: models::ListSipCredentialResponse = serde_json::from_str(
        r#"{"credentials":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_credential_list() {
    let response: models::ListSipCredentialListResponse = serde_json::from_str(
        r#"{"credential_lists":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_credential_list_mapping() {
    let response: models::ListSipCredentialListMappingResponse = serde_json::from_str(
        r#"{"credential_list_mappings":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_domain() {
    let response: models::ListSipDomainResponse =
        serde_json::from_str(r#"{"domains":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_ip_access_control_list() {
    let response: models::ListSipIpAccessControlListResponse = serde_json::from_str(
        r#"{"ip_access_control_lists":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_ip_access_control_list_mapping() {
    let response: models::ListSipIpAccessControlListMappingResponse = serde_json::from_str(r#"{"ip_access_control_list_mappings":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#).unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_sip_ip_address() {
    let response: models::ListSipIpAddressResponse = serde_json::from_str(
        r#"{"ip_addresses":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}
#[test]
fn adapter_list_transcription() {
    let response: models::ListTranscriptionResponse = serde_json::from_str(
        r#"{"transcriptions":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#,
    )
    .unwrap();
    let page = PageResponse::into_page(response);
    assert_eq!(page.items.len(), 1);
    assert_eq!(
        page.next_page_uri.as_deref(),
        Some("/next?PageToken=a%2Fb%2Bc%3D")
    );
}

fn configuration(server: &MockServer) -> Configuration {
    let client = HttpClient::new(ClientConfiguration {
        credentials: Credentials::account_token(
            AccountSid::new("ACaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa").unwrap(),
            SecretString::from("synthetic-token".to_owned()),
        ),
        base_url: Url::parse(&server.uri()).unwrap(),
        connect_timeout: Duration::from_secs(1),
        request_timeout: Duration::from_secs(2),
        retry_policy: RetryPolicy::conservative(),
        allow_http_for_tests: true,
    })
    .unwrap();
    Configuration::new(client)
}

#[tokio::test]
async fn generated_pager_handles_zero_and_multiple_pages_and_reserved_tokens() {
    let server = MockServer::start().await;
    let empty: models::ListCallResponse = serde_json::from_str(r#"{"calls":[]}"#).unwrap();
    assert!(follow(configuration(&server), empty).next().await.is_none());

    Mock::given(method("GET"))
        .and(path("/next"))
        .and(query_param("PageToken", "a/b+c="))
        .and(header_exists("authorization"))
        .respond_with(ResponseTemplate::new(200).set_body_raw(
            r#"{"calls":[{},{}],"next_page_uri":null}"#,
            "application/json",
        ))
        .expect(1)
        .mount(&server)
        .await;
    let first: models::ListCallResponse =
        serde_json::from_str(r#"{"calls":[{}],"next_page_uri":"/next?PageToken=a%2Fb%2Bc%3D"}"#)
            .unwrap();
    let mut pager = follow(configuration(&server), first);
    assert!(pager.next().await.unwrap().is_ok());
    assert!(pager.next().await.unwrap().is_ok());
    assert!(pager.next().await.unwrap().is_ok());
    assert!(pager.next().await.is_none());
}

#[tokio::test]
async fn generated_pager_rejects_loops_and_cross_origin_links() {
    let server = MockServer::start().await;
    Mock::given(method("GET"))
        .and(path("/loop"))
        .respond_with(ResponseTemplate::new(200).set_body_raw(
            r#"{"calls":[],"next_page_uri":"/loop"}"#,
            "application/json",
        ))
        .expect(1)
        .mount(&server)
        .await;
    let looped: models::ListCallResponse =
        serde_json::from_str(r#"{"calls":[],"next_page_uri":"/loop"}"#).unwrap();
    assert!(
        matches!(follow(configuration(&server), looped).next().await, Some(Err(Error::Validation(message))) if message.contains("repeated"))
    );

    let foreign: models::ListCallResponse =
        serde_json::from_str(r#"{"calls":[],"next_page_uri":"https://evil.invalid/next"}"#)
            .unwrap();
    assert!(
        matches!(follow(configuration(&server), foreign).next().await, Some(Err(Error::Validation(message))) if message.contains("cross-origin"))
    );
}
